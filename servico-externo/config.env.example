# Configurações SMTP para envio de e-mails
# Copie este arquivo para .env e preencha com suas credenciais

# Servidor SMTP (padrão: Gmail)
SMTP_SERVER=smtp.gmail.com

# Porta SMTP (587 para TLS, 465 para SSL)
SMTP_PORT=587

# Usuário/email para autenticação SMTP
SMTP_USERNAME=seu-email@gmail.com

# Senha ou App Password (para Gmail, use App Password)
SMTP_PASSWORD=sua-senha-ou-app-password

# E-mail remetente (padrão: mesmo que SMTP_USERNAME)
SMTP_FROM_EMAIL=seu-email@gmail.com

# Usar TLS (true/false)
SMTP_USE_TLS=true

